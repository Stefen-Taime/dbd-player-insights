# =============================================================================
# Streamlit Docker Compose — generated by Ansible
# =============================================================================

services:
  streamlit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamlit
    env_file: .env
    ports:
      - "{{ streamlit_port }}:8501"
    restart: unless-stopped
